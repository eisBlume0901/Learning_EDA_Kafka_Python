Docker commands
- docker-compose up - to start and run the Docker containers define din the docker-compose.yaml file

Kafka commands (exec mode)
- kafka-topics --list --botstrap-server localhost:9092 - to view the list of all available Kafka topics on the broker running at the localhost:9092